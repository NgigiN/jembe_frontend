# Config for `dependency_validator` (run in CI as the "Verify dependencies" step).
#
# drift 2.32 (R2-06/D15) dropped the old `sqlcipher_flutter_libs` pin recipe in
# favour of native-assets SQLite3MultipleCiphers, so the previous ignore
# entries (`sqlcipher_flutter_libs`, and the `drift`/`drift_dev` "don't pin a
# version range" suppressions for the `>=2.20.0 <2.32.0` pin) are gone: those
# packages now use plain caret constraints the validator is happy with, and
# `sqlite3` is a genuine direct `import` in app_database.dart.
ignore: []
